Optional DSPy optimized models can be placed here.

The application does not load a model from this directory unless
`rag.optimized_model_path` is set in `config.yaml`. Keep it unset for
domain-neutral notebooks.
